# Minimal / required for baseline behavior
spacy>=3.0
transformers>=4.0
torch>=1.10        # or install CPU-only torch if no GPU; see README
datasets>=2.0

# Optional / strongly recommended for better clinical NER and keywords
scispacy>=0.5.0         # optional, for biomedical NER (requires model install)
keybert>=0.6.0          # optional, better keyword extraction
sentence-transformers>=2.2.2  # KeyBERT dependency
rake-nltk               # fallback keyword extractor
nltk                    # for some text utilities

# Optional medSpaCy
medspacy>=0.1.0         # optional

# (If using GPU, ensure you have CUDA-compatible torch installed)
